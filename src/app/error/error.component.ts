import { Component } from '@angular/core';

@Component({
  selector: 'app-error',
  templateUrl: '../error/error.html',
  styleUrls: ['../error/error.scss']
})

export class ErrorComponent {
 
  constructor( ) {
    }
}
